//    if (!(cin >> data)) {(where int data )
//         cout << "Invalid input. Please enter an integer." << endl;
//         // Handle the error, clear the input buffer
//         cin.clear();
//         cin.ignore(numeric_limits<streamsize>::max(), '\n');
//         return build(root);
//     }
